# Supabase credentials (server-only — never expose to the browser)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=your_service_role_key

# Public base URL of this deployment (used in /info response)
NEXT_PUBLIC_BASE_URL=https://your-deadline-manager.vercel.app
